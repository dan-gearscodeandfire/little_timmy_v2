# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
*.egg

# Models (large binary files)
models/*.onnx
models/*.onnx.json
models/tts/
# Speaker voiceprints + id map are runtime-generated state, never tracked
# (covers *.npy and variant suffixes like .npy.pre_pipeline / .npy.disabled).
models/speaker/

# IDE
.vscode/
.idea/

# Logs
*.log

# OS
.DS_Store
Thumbs.db

# Secrets / env
.env
*.secret

# Temp
persona_tuning/

# Backup files
*.bak.*

# Runtime feedback inbox (mirrors persona_tuning convention)
feedback_inbox.jsonl

# Runtime state
data/

# LT-OS dashboard session logs (JSON, not *.log)
little_timmy_os/logs/
face_db_provenance.json

# voice-capture artifacts (PII real-voice WAVs + synthetic cal audio; regenerable, ~52MB) — keep out of git
ops/*_captures/
